const sayHello = (name) => {
  console.log(`Sup whichu ${name}?`);
};

sayHello("John");

// console.log(process);

process.stin.on("data", (input) => {
  const userInput = input.toString().trim();
  console.log(`You entered: ${userInput}`);
});

process.exit();

// NE E DOVRSHENO
